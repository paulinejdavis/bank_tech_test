const Transaction = require('../lib/transaction')

describe('Transaction', () => {
    it('makes a deposit', () => {
        const myAccount = {
            newBalance: (total) => {},
            getBalance: () => {1000}
        };
    });

});